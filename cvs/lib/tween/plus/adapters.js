/*
* 把每个传入元素抽象理解成一个矩形（因为手机屏幕是矩形）
* 元素从矩形某个位置开始从小变大，直到“填充满”整个矩形，即元素的宽高大于等于抽象成的“矩形的宽高
*/

/**
 * canvasElement 类型的元素
 * 将传入的元素适配成矩形，即转换“矩形”的宽高
 * @param {Object} element 
 */
var Adapters = function(element){
    
};
